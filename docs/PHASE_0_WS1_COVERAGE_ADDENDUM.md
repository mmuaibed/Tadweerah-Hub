# WS1 Coverage Addendum

**Audit date:** 2026-07-02
**Scope:** Closes out the four skipped/partial items listed in the main WS1 report (`PHASE_0_PROJECT_FILES_INTAKE_AND_CURRENT_UNDERSTANDING.md`). Read-only only — no code edits, no commits, no deploy, no DB access, no deletion, no secret values printed. This addendum does not redo the full report.

| Skipped/partial item | What was additionally reviewed | New evidence found | Does it change the WS1 report? | Required update | Status |
|---|---|---|---|---|---|
| **1. Two large sustainability planning docs (previously ~40-50% read)** | Full end-to-end read of `SUSTAINABILITY_REPORTS_ENGINEERING_DISCOVERY.md` (2,308 lines) and `SUSTAINABILITY_REPORTS_IMPLEMENTATION_PLAN.md` (1,722 lines) — print/PDF design, CO2e scope, risk register, open questions, latest recorded phase. | (a) **Doc/code drift confirmed:** both docs stop recording progress at Gate 3/SIR-2B (2026-06-25) with zero content on SIR-2C/2D/3A/3B — yet the code already has a working branded PDF print page at `/reports/sustainability/:id/print` (commits reference "SIR-3B"). The docs can no longer answer "is this done?" — only the code can. (b) **Internal self-contradiction:** Plan §13 (open questions) still recommends "disclaimer-only" for processed-output flagging, but an earlier v1.1 addendum in the *same file* overrides this to mandate a structural `is_processed_output` flag — the open-questions table was never reconciled with the addendum. (c) CO2e confirmed fully out-of-scope/placeholder-only, with explicit rules never to use "carbon credit/offset" language. (d) 15-item risk register surfaced, incl. two flagged 🔴 (greenwashing liability; cross-transaction double-counting, "remains High until flag deployed and tested"). (e) No new information directly resolving the "40/35/5" cross-screen discrepancy, but the docs' design intent ("no silent balancing," gaps must always be shown) is relevant context. | **Yes, materially.** Doc/code drift and the internal self-contradiction are new audit-relevant findings, not just added detail. | Add to Cross-Cutting Findings: "Sustainability planning docs are stale vs. shipped print/PDF code" and "Plan §13 processed-output-flag contradiction unresolved in the doc itself." Add the two 🔴 risk-register items to founder review. | **Closed** (fully read). Downstream hypotheses (flag actually enforced? PDF Arabic spike validated before shipping?) remain open for later verification. |
| **2. Deep-read of previously unread page files** (`participations.tsx`, `pending-actions.tsx`, `company-profile.tsx`, `MasterDataTab.tsx`, `contracts.tsx`, `contract-new.tsx`, `contract-detail.tsx`, `company-capabilities.tsx`, `members.tsx`) | Full read of all 9 files (~5,500 lines total), with attention to state machines, API calls, and role logic. | (a) **At least 4 non-unified role vocabularies coexist** with no bridging logic: `generator/receiver/transporter` (company profile), `seller/buyer` (Contract Lite), `producer/buyer` (marketplace deals), `owner/admin/member` (team membership). (b) **Contract Lite has no payment step at all** and never uses generator/receiver/transporter — it is more structurally separate from the marketplace deal flow than previously understood, with its own nested shipment sub-machine (`planned→dispatched→received→closed/cancelled`) gated by a 5-way `weight_policy` enum. (c) `MasterDataTab.tsx` is labeled "Editable Lists" in its own UI but has **zero working edit/save actions** and performs no independent access check — purely decorative, fully trusts the parent admin page's gate. (d) `participations.tsx` has a stale local status type missing 4 of the 8 real deal states, silently mislabeling them as generic "in progress." (e) `contract-new.tsx` has a fully-coded but UI-hidden "Attachment URL" field, intentionally disabled ("Hidden for Phase 1 UAT") — not a bug. (f) A legacy "assume seller is creator" fallback (for contracts predating a schema column) is duplicated across 3 files. | **Yes.** Role-vocabulary fragmentation and Contract Lite's structural separation are new platform-map-level findings. | Update "Company Roles" and "Deals/Contracts" sections of the platform map to note the 4 role vocabularies and the payment-free Contract Lite structure. Add MasterDataTab non-functionality and the `participations.tsx` stale type as new hypothesis/bug candidates. | **Closed.** |
| **3. Visual journey exports (.drawio / .mmd / .svg / .pdf / .png)** | Read the text-based export manifests (`EXPORT_MANIFEST.md`, `README_EXPORTS.md`) and one representative `.mmd` source file. Confirmed the canonical source for the pre-phase-3b exports is `08A_VISUAL_DIAGRAMS.md` (markdown, already read in the original pass) — the `.mmd` files are its plain-text mirror (Arabic-encoding already fixed). Did not open SVG/PDF/`.drawio` binaries — impractical and unnecessary, since they are snapshots of already-read text. | The `.mmd` evidence-map diagram explicitly re-confirms the "40/35/5" risk in diagram form ("No fix inferred in Pre-Phase 3-B") — consistent with, not new versus, prior findings. **Genuinely new:** discovered a *separate* document, `docs/exports/operational-truth-v5/` — a 12-page PDF whose own README self-labels it **"SOURCE OF TRUTH... full fidelity... for Strategic Partner discussions,"** sourced from `Tadweerah_Operational_Truth_Workflows_v5.drawio`. This is image/PDF content and was **not opened**. | **Minor.** The pre-phase-3b exports don't change anything (already covered via their canonical markdown). But the un-reviewed "Operational Truth v5" PDF — a document that `WORKFLOW_ARCHITECTURE.md` itself defers to as more canonical than itself — is a real, newly-identified coverage gap. | Add "Operational Truth Workflows v5 PDF (12 pages) not yet reviewed" to Source-of-Truth risks and to missing inputs/blockers. Recommend a later pass with visual inspection (this is a binary/visual document; a text-only audit cannot close it). | **Partially closed.** Pre-phase-3b exports: closed (text was sufficient). Operational Truth v5 PDF: **Not yet checked — needs later, visual-capable verification.** |
| **4. `expire-deals.ts` scheduling** | Repo-wide grep for scheduler/cron/`setInterval` references tied to the expire-deals job. | **Resolved, code-confirmed:** `artifacts/api-server/src/index.ts:29` — `setInterval(() => void expireStaleDeals(), 60 * 60 * 1000)`. The job runs every hour via an in-process JavaScript timer, not an external Cloud Scheduler/cron job. | **Yes** — resolves a "Not yet checked" item to "Code-confirmed." | Update the Deals section: job scheduling is resolved. Add one new, low-confidence hypothesis: because this is `setInterval`-based (not a distributed lock or external cron), if the backend were ever scaled to multiple concurrent instances, the job could fire redundantly per instance. No evidence of multi-instance deployment was reviewed — this is a hypothesis, not a finding. | **Closed** (scheduling mechanism); new hypothesis added to the register, not yet verified. |

## Impact on Existing WS1 Report Sections

- **Initial Platform Map:** Update Company Roles / Deals / Contracts to state plainly that four separate role vocabularies exist with no bridging logic, and that Contract Lite is a payment-free, structurally distinct workflow from the marketplace deal flow.
- **Known User Journeys:** `pending-actions.tsx` revealed a richer "your turn" action vocabulary than previously documented, including a transporter-selection sub-flow (`choose_transport`, `transport_pending_producer`, `transport_pending_buyer`) nested inside the deal journey.
- **Sustainability Reports understanding:** The print/PDF feature is more code-mature than the planning docs suggest (docs stopped recording at SIR-2B; code shows SIR-3B branded print already shipped) — treat the docs as stale for "is it done" questions from here forward, and rely on code + the two flagged 🔴 risk items for founder review.
- **Source-of-Truth risks:** Add two entries — the sustainability Implementation Plan's own header ("current phase: SR-1D") contradicts its own gate log and should not be trusted at face value; and the Operational Truth Workflows v5 PDF is a self-declared source-of-truth document that remains unreviewed.
- **Audit Hypothesis Register:** Add — role-vocabulary fragmentation as a boundary-bug risk; `MasterDataTab.tsx` non-functionality; `participations.tsx` stale status type; possible redundant `expire-deals` execution under multi-instance deployment (low confidence); the unresolved processed-output-flag contradiction in the sustainability plan.

## Final Recommendation

**WS1 can be closed with limitations.**

All four skipped/partial items were addressed with direct file/code evidence except one: the `Tadweerah_Operational_Truth_Workflows_v5.pdf` (12 pages, self-declared "source of truth") is a visual document that a text-based audit cannot close — it is carried forward as an explicit "Not yet checked" item for a later phase with visual-inspection capability, not treated as resolved. Everything else closed with new, evidence-backed findings that update — but do not contradict the core structure of — the original WS1 report.
