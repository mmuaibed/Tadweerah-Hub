We have completed many tests and the current database contains incomplete test listings, deals, transport requests, support issues, and Replit-created test accounts.

Before using the platform for a CEO demo / strategic partner demo, we want to clean the database and start fresh.

Do not change code.
Do not change schema unless absolutely required.
Do not refactor.
Do not touch business logic.
Do not touch Git.
Do not touch Secrets.

This is a controlled database cleanup only.

Important:
Before deleting anything, create a database backup/export or at least provide a clear pre-cleanup snapshot summary.

Goal:
Remove test/dirty operational data and keep the platform structure, reference data, and admin access intact.

Please do this in three phases:

==================================================
Phase 1 — Assessment only
==================================================

First inspect the database and report counts for:

- users / app users if stored locally
- companies
- company_members
- waste_listings
- listing_offers
- deals
- transport_requests
- transport_quotes
- contracts
- contract shipments, if any
- issue_reports
- audit logs
- uploaded/local listing images if trackable
- lookup/reference tables:
  - material categories
  - capabilities/services
  - cities if any
  - statuses/lookups if any

Identify which tables are:
A) operational/test data to clean
B) reference/seed data to keep
C) admin/security data to preserve

Do not delete yet until you provide the plan.

==================================================
Phase 2 — Cleanup plan
==================================================

Prepare a safe cleanup plan.

Required preservation:
- Keep DB schema.
- Keep lookup/reference data.
- Keep material categories and subcategories.
- Keep capabilities/services.
- Keep admin access for mmuaibed@stc.com.sa.
- Keep environment variables and secrets.
- Keep GitHub code untouched.
- Keep migrations.

Data to delete/reset:
- all test listings
- all offers
- all deals
- all transport requests
- all transport quotes
- all test contracts/shipments
- all issue reports/test support tickets
- all test companies and members except the admin user/company handling if needed
- any test audit logs if safe, or keep audit logs if deleting them may break traceability

Clarify whether Clerk users can be deleted from Clerk or only unlinked from the app DB.
Do not delete Clerk users unless explicitly safe and approved.

If deleting app users/company records affects foreign keys, use the correct deletion order.

==================================================
Phase 3 — Execute cleanup after confirmation
==================================================

After I approve the cleanup plan, execute the cleanup.

Important:
- Use transactions where possible.
- Respect foreign key order.
- Do not truncate reference tables.
- Do not reset migrations.
- Do not delete admin configuration.
- Do not delete secrets.
- Do not delete code.
- Do not push Git unless code was changed, which should not happen.

After cleanup, verify:

1) Admin login still works for:
   mmuaibed@stc.com.sa

2) Admin page opens.

3) Admin tabs open:
   - companies
   - deals
   - transport requests
   - reports
   - customer issues

4) User onboarding still works.

5) New company registration can be created.

6) New listing can be created.

7) Marketplace loads empty or clean.

8) Reports load with zero/clean data.

9) No old test deals/transport/support issues appear.

10) API health works:
   curl -s http://localhost:80/api/healthz

11) Typechecks:
   pnpm --filter @workspace/api-server run typecheck
   pnpm run typecheck

Final report should include:
1) backup/snapshot confirmation
2) tables cleaned
3) rows deleted per table
4) tables preserved
5) admin access confirmation
6) remaining clean database counts
7) typecheck and health result
8) any known limitation