Important final admin email change before migration.

We want to remove mmuaibed@stc.com.sa as the Tadweerah admin email and replace it with:

mmuaibed@outlook.com

Goal:
mmuaibed@outlook.com should become the official admin email with the same admin capabilities.
mmuaibed@stc.com.sa should no longer have admin access.

Do not change business logic.
Do not change VAT, deals, reports, transport, support issues, transporter quotes, or DB schema unless absolutely required.
Do not delete Clerk users.
Do not expose secret values.

Please do this in careful phases:

==================================================
Phase 1 — Audit
==================================================

Search for mmuaibed@stc.com.sa in:

1) Database tables
2) Environment variables / Secrets names and values if safely inspectable
3) Frontend code
4) Backend code
5) Markdown/docs files
6) Admin allowlist logic
7) Any generated env reference files

Report where it appears.

Also search for mmuaibed@outlook.com and report whether it already appears anywhere.

Do not change anything before reporting.

==================================================
Phase 2 — Update
==================================================

If admin access is controlled by VITE_TADWEERAH_ADMIN_EMAILS, update it so that:

VITE_TADWEERAH_ADMIN_EMAILS=mmuaibed@outlook.com

Remove mmuaibed@stc.com.sa from admin allowlist.

If there are any DB records containing mmuaibed@stc.com.sa, update or delete them only if they are operational/admin-related and safe.

Do not delete Clerk users.
If a Clerk user account for mmuaibed@outlook.com does not exist yet, tell me clearly that I need to login/sign up with that email first.

If any documentation or env reference file contains the old admin email as an example, update it to mmuaibed@outlook.com.

==================================================
Phase 3 — Verify
==================================================

Verify:

1) mmuaibed@outlook.com is allowed as admin.
2) mmuaibed@stc.com.sa is not allowed as admin.
3) Admin page opens for the new admin email after login.
4) Admin API key flow still works.
5) Admin tabs still load:
   - companies
   - deals
   - transport requests
   - reports
   - customer issues
6) Support email flow is not broken.
7) Database remains clean.
8) No test data was created.
9) Typechecks pass:
   - pnpm --filter @workspace/api-server run typecheck
   - pnpm run typecheck
10) Health check passes:
   - curl -s http://localhost:80/api/healthz

==================================================
Phase 4 — New backup package
==================================================

After verification, create new final backup files again:

1) Clean DB backup:
   tadweerah-final-clean-after-admin-email-update-20260521.tar.gz

2) Updated env variables reference:
   tadweerah-env-variables-final-20260521.txt
   tadweerah-env-variables-final-20260521.md

The env files must include variable names only, no secret values.

3) If code/docs changed, commit the changes with a clear message:
   Update final admin email to Outlook

4) Push to GitHub:
   git push origin main

5) Verify GitHub sync:
   - git status
   - git status -sb
   - git log -1 --oneline
   - git rev-parse HEAD
   - git rev-parse origin/main
   - confirm local main and origin/main are identical

Final report:
1) where old email was found
2) what was changed
3) whether any DB rows were changed
4) new admin email confirmation
5) old admin email access removed confirmation
6) backup files created
7) GitHub sync confirmation
8) typecheck and health result