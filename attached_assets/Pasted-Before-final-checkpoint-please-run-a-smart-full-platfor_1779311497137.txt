Before final checkpoint, please run a smart full-platform QA test.

Do not implement new features.
Do not refactor.
Do not change DB schema.
Do not touch VAT, deals, offers, transport lifecycle, reports logic, admin auth, or transporter quote logic unless a blocking bug is found.

Goal:
Verify that the full Tadweerah MVP is stable after the recent updates:
- VAT
- transport responsibility
- Google Maps location
- admin account
- reports
- transporter quotes
- eManifest readiness display
- i18n/formatting

Please run a smart QA pass using low-cost checks first:
- API/route checks
- existing data checks
- minimal UI smoke tests
- no unnecessary screenshots
- no broad exploratory testing

==================================================
1) Core app pages
==================================================

Verify these pages load without runtime errors, hook errors, or obvious i18n keys:

- Dashboard
- Marketplace
- Listing detail
- Listing creation flow step 1 / 2 / 3
- My Listings
- Participations / Offers
- Pending Actions
- Deal page
- Transport Requests
- Reports page /reports
- Admin page /admin
- Admin Companies tab
- Admin Deals tab
- Admin Transport Requests tab
- Admin Reports tab

For each:
- confirm no runtime overlay
- confirm no console error that blocks usage
- confirm no visible untranslated keys like deal.status.xxx or dashboard.xxx
- confirm page layout is not broken

==================================================
2) Deal flow smoke test
==================================================

Using existing test data if possible, verify:

- Listing detail opens
- Buyer can submit/see offer flow without page crash
- Seller offer review page opens
- Deal page opens as buyer/seller
- VAT display is correct:
  - amount before VAT
  - VAT 15%
  - total including VAT
- Report/print deal page opens if accessible
- Deal title uses subcategory + quantity/unit where available
- No 10,000.008 style rounding issue appears

Do not create many new deals unless needed.

==================================================
3) Location / Google Maps
==================================================

Verify:
- material_location_address displays correctly
- material_location_notes displays separately
- google_maps_url is used first if present
- if google_maps_url is missing, Maps fallback uses material_location_address only
- city and site details are not appended to Maps query
- links open in new tab with rel=noopener noreferrer

==================================================
4) Reports QA
==================================================

Company reports:
- /reports loads
- role filters work: all / seller / buyer
- date/status/city filters work
- summary cards load
- table rows load
- CSV export works
- numbers use Western Arabic numerals
- SAR displays as 10,000 ر.س in Arabic
- no raw UUID if business reference/manifest ref is available

Admin reports:
- Admin Reports tab loads
- filters work
- summary cards load
- table rows load
- CSV export works
- transport status is translated, not raw pending/accepted/etc
- no i18n keys visible

==================================================
5) Admin QA
==================================================

Verify:
- mmuaibed@stc.com.sa is treated as Admin
- admin page requires allowed email
- admin API routes require ADMIN_API_KEY
- Companies tab loads
- Deals tab loads
- Transport Requests tab loads
- Reports tab loads
- logout works and redirects properly
- no admin data visible after logout

==================================================
6) Transporter quotes QA
==================================================

Do low-cost verification only.

Verify:
- transport_quotes table exists
- admin quote panel loads
- if no quotes exist, empty state says:
  Arabic: لا توجد عروض نقل حالياً
- existing quote routes are registered and protected
- selecting quote does NOT assign transporter_company_id
- selecting quote does NOT change transport_request status
- selecting quote does NOT auto-reject competing quotes
- transporter quote data does not appear in buyer/seller reports
- transporter routes do not expose deal financial values

If real transporter login is not available, use code/API/DB verification only and clearly state that real transporter UI login remains untested.

==================================================
7) Security / privacy checks
==================================================

Verify:
- company report returns only authenticated company’s deals
- admin reports require X-Admin-Key
- transporter sees only own quotes
- transporter does not see other transporter quotes
- transporter does not see deal financial values
- non-admin cannot access /admin
- non-transporter cannot access transporter-only views if implemented

==================================================
8) Technical checks
==================================================

Run:
- pnpm --filter @workspace/api-server run typecheck
- pnpm run typecheck
- curl -s http://localhost:80/api/healthz

Also provide:
- git status
- list of uncommitted files
- whether current state is ready for final checkpoint

==================================================
Final report format
==================================================

Please provide a table:

Area | Result | Notes | Blocking?

Areas:
1. Core pages
2. Deal flow
3. VAT
4. Location/Maps
5. Reports
6. Admin
7. Transport requests
8. Transporter quotes
9. Security/privacy
10. Typecheck/health
11. Git/checkpoint readiness

If you find:
- minor display issue: report it
- blocking runtime issue: fix only after explaining
- logic issue touching lifecycle/security: stop and ask before changing

Do not implement new features during this QA pass.