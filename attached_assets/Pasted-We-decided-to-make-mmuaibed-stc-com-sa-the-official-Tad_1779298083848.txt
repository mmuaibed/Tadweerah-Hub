We decided to make mmuaibed@stc.com.sa the official Tadweerah Admin/Ops account.

Reason:
I logged into this email and found it is currently shown as “الكابلات المميزة”, with no deals or offers. For better governance and data privacy, this account should not remain as a normal company account. It should become the dedicated Tadweerah Admin/Ops account.

Important:
Do not implement blindly. First verify that this account has no critical operational records.

Step 1 — Verify current account data
Please inspect the database and confirm for mmuaibed@stc.com.sa / its Clerk user ID:
1) linked company name
2) company_id
3) company_members record
4) listings created by this company/user
5) offers submitted
6) deals where this company is buyer or seller
7) transport requests linked to it
8) contracts linked to it
9) any other operational data that would be affected

If there are active listings, offers, deals, transport requests, or contracts, stop and report before making changes.

Step 2 — If account is clean, convert to Tadweerah Admin/Ops
If the account has no important operational records, implement the safest conversion:

A) Admin identity
- Show the account name as:
  “مدير تدويرة”
- Account type/role should display as:
  “Tadweerah Admin” or “Tadweerah Operations”
- This account should not appear as a normal buyer/seller company in the user interface.

B) Access control
- Only explicitly approved admin emails should access /admin.
- mmuaibed@stc.com.sa should be included as the admin email.
- If the admin allowlist env var is empty, block everyone from /admin.
- Do not allow public access to /admin.

C) Backend security
- Keep all /api/admin/* routes protected by ADMIN_API_KEY / X-Admin-Key.
- Do not rely only on frontend email checks.
- Admin mutations must remain protected server-side.

D) Company relationship
- If the user is currently linked to “الكابلات المميزة” and there are no operational records, either:
  1) unlink this user from that company safely, or
  2) mark the admin account as platform_admin so it bypasses normal company context in admin pages.
- Do not delete the company itself unless you confirm it is only test data and safe to remove.
- Do not delete any historical or operational records.

E) Admin dashboard capabilities
The admin account should access the existing /admin dashboard and see:
- Companies
- Deals
- Transport requests
- Issue reports if already available
- Audit log if already available
- Contracts if already available

For capabilities not yet available, do not build a large new module now. Just report gaps:
- all listings view
- full transport status management
- ops feed
- internal notes

Step 3 — Required env vars
Confirm exact values/steps needed:
- VITE_TADWEERAH_ADMIN_EMAILS=mmuaibed@stc.com.sa
- ADMIN_API_KEY=<strong secret>

Step 4 — Validation
After implementation, run:
- pnpm --filter @workspace/api-server run typecheck
- pnpm run typecheck
- curl -s http://localhost:80/api/healthz

Final report must include:
1) what was found for the current account
2) whether any data was changed or unlinked
3) how mmuaibed@stc.com.sa is now treated as Admin/Ops
4) how /admin access is restricted
5) confirmation that backend admin routes still require ADMIN_API_KEY
6) changed files
7) any remaining admin dashboard gaps

Strict constraints:
- Do not touch VAT.
- Do not touch offers.
- Do not touch deals lifecycle.
- Do not touch transport workflow.
- Do not touch MWAN.
- Do not touch pricing.
- Do not touch material/location display.
- Do not implement new large admin modules without approval.