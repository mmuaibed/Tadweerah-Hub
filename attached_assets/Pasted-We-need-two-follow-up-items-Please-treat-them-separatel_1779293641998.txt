We need two follow-up items. Please treat them separately.

Part A can be implemented/verified if safe.
Part B requires assessment first before implementation.

Do not touch VAT calculation.
Do not touch offer logic.
Do not touch pricing_model.
Do not touch offer_subtotal_amount.
Do not touch deal lifecycle states.
Do not touch transport state machine.
Do not touch MWAN integration.
Do not implement multi-material listings.

==================================================
Part A — Verify and standardize material location display
==================================================

The new fields were added:
- material_location_address
- google_maps_url

Now please verify and complete the display rule:

Whenever the system displays the sale/material city, it should also display the material location address if available, and show a Google Maps link/button if google_maps_url exists.

Required display behavior:

1) Anywhere city appears in listing/deal context, show:
- City
- National Address / Material Location Description, if available
- “Open in Google Maps” button/link, if google_maps_url exists

Arabic labels:
- المدينة
- العنوان الوطني / وصف موقع المواد
- فتح الموقع على Google Maps

English labels:
- City
- National Address / Material Location Description
- Open in Google Maps

2) Apply to:
- listing creation review/summary if present
- listing detail page
- deal page header or deal info section
- buyer deal view
- seller deal view
- printed deal report
- pending actions card if location is shown there
- any compact listing/deal info panel that currently shows city
- MWAN/eManifest readiness panel only if location is already displayed there; do not expand MWAN logic

3) Google Maps behavior:
- The Google Maps link should be clickable.
- It should open in a new tab.
- Do not embed Google Maps.
- Do not use Google Maps API.
- Do not calculate distance.
- Validate/sanitize enough to avoid unsafe links.
- If no google_maps_url is provided, show city/address only.

4) Important:
- Do not add new fields unless absolutely required.
- Do not rename existing fields.
- Do not break old listings.
- Existing listings with null address/maps should continue to show city only.

Please inspect and confirm:
A) all places where city is currently displayed
B) whether material_location_address appears next to city in each place
C) whether google_maps_url appears as a clickable link/button in each place
D) files changed if any

==================================================
Part B — Tadweerah Admin/Ops account assessment first
==================================================

We need a proper Tadweerah Admin/Ops account.

Preferred email:
mmuaibed@stc.com.sa

Business goal:
This account should become the Tadweerah Admin/Ops account, displayed as:
- Name: مدير تدويرة
- Account type/role: Admin / Tadweerah Operations

The Admin/Ops account should have a page/dashboard where it can:
1) View all platform activity
2) View all listings
3) View all deals
4) View all transport requests
5) Track and complete Tadweerah transport requests operationally
6) View new company/user registrations
7) Verify and approve/reject new company accounts
8) Update company approval/license status if already supported
9) Access operational follow-up items and pending actions across the platform

Important:
Do not implement this immediately before assessment.
Do not destructively delete or reset existing data for mmuaibed@stc.com.sa.
Do not break any existing company/user/listing/deal relationships for this account.
Do not convert the account into a seller/buyer/admin hybrid in a way that breaks normal flows.
Do not expose admin routes publicly.
Do not rely only on frontend hiding for sensitive backend operations.

Please first inspect and report:

1) Current auth model
- Is Clerk used?
- Where is user email available?
- Is there a users table?
- Is there a company_users or membership table?
- Is there an existing role field?
- Is there an existing admin flag or admin role?

2) Current admin implementation
- Does /admin already exist?
- What does it show now?
- Which backend routes exist under /api/admin/*?
- Are they protected by ADMIN_API_KEY / x-admin-key?
- Are company approval routes already available?
- Are transport request management routes already available?

3) Current account state
For mmuaibed@stc.com.sa:
- Does this user currently exist?
- Is it linked to a company?
- What company/role/status is attached to it?
- Would changing its role/name affect existing test data?

4) Recommended safest implementation approach
Choose the safest option:

Option A — Admin allowlist + display name override:
- TADWEERAH_ADMIN_EMAILS or VITE_TADWEERAH_ADMIN_EMAILS includes mmuaibed@stc.com.sa
- Display this user as “مدير تدويرة” in admin context
- Keep existing user/company data untouched
- Backend admin APIs remain protected by ADMIN_API_KEY

Option B — Add platform_admin / tadweerah_ops role:
- Only if a user role model already exists or can be added safely
- Must be backend-enforced, not frontend only

Option C — Platform company/admin organization:
- Only if current architecture supports it safely
- Admin should not be treated as a normal buyer/seller company

5) Admin dashboard gap analysis
For each capability below, state whether it already exists, needs small patch, or needs later phase:
- View all listings
- View all deals
- View transport requests
- Update transport request status
- View companies/users
- Approve/reject companies
- Update license status
- View pending operational actions
- Internal notes / ops notes

6) Security plan
Confirm:
- Which routes are backend-protected
- Which env vars are required
- Whether mmuaibed@stc.com.sa can access admin UI
- Whether ADMIN_API_KEY is still required for mutations
- Any security gaps

Do not implement Part B until I approve the plan.

After Part A verification or changes, run:
- pnpm --filter @workspace/api-server run typecheck
- pnpm run typecheck
- curl -s http://localhost:80/api/healthz

Final response should include:
1) Location display verification results
2) Changed files for Part A, if any
3) Admin/Ops assessment for Part B
4) Recommended implementation plan for the admin account
5) Risks before implementation